# StudyMate Environment Configuration
# App runs on Windows, Database in Docker containers

# Environment
NODE_ENV=development

# Server Configuration
PORT=3000
HOST=localhost

# Database Configuration (Docker PostgreSQL)
# Connect to PostgreSQL running in Docker container
DB_HOST=localhost
DB_PORT=5432
DB_NAME=studymate_dev
DB_USER=studymate
DB_PASS=studymate123

# Redis Configuration (Docker Redis)  
# Connect to Redis running in Docker container
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=redis123

# Authentication & Security
JWT_SECRET=studymate_super_secret_key_for_development_2024_make_it_long_and_secure
JWT_EXPIRE=24h
JWT_REFRESH_EXPIRE=7d
SESSION_SECRET=studymate_session_secret_key_2024_change_in_production

# Email Configuration (Optional)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password

# File Upload
MAX_FILE_SIZE=10MB
UPLOAD_PATH=./uploads

# AI Integration (Optional - Uncomment to enable)
# OPENAI_API_KEY=your_openai_api_key_here
# GEMINI_API_KEY=your_gemini_api_key_here

# Logging
LOG_LEVEL=info
LOG_FILE=./logs/app.log

# Elasticsearch Configuration (Docker)
ELASTICSEARCH_ENABLED=true
ELASTICSEARCH_HOST=localhost
ELASTICSEARCH_PORT=9200
ELASTICSEARCH_INDEX_PREFIX=studymate
ELASTICSEARCH_LOG_LEVEL=info

# Rate Limiting
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX=100
